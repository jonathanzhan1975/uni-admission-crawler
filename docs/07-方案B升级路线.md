# 方案 B 升级路线 — 39 所 985 全覆盖

> **决策**：A（codex 全自动审计）+ B（仅 P0/P1 做 backup）+ C（立即启动，5/12 起）
>
> **目标**：在 POC 7 天验收并行进行的同时，启动审计与实施，**5/25 前 39 所全覆盖上线**。
>
> 范围与 POC 一致：**仅本科招生**，研究生招生不在范围内（按 docs/02 §1.3）。

---

## 1. POC 教训复盘（指导原则）

| POC 现象 | 根因 | 升级期对策 |
|---|---|---|
| SJTU jwc/yzb 推 2 条 | RSSHub 路由名 ≠ 本科招生 | 不能凭路由名信任，必须**实际拉取后判定**内容主题 |
| 复旦 ao 改版后 selector 失效 | 没用真实页面写 selector | 每所大学在登记前必须 **`curl` 真实页面 + 至少 1 次 live 测试** |
| rsshub.app 被 Actions IP ban | 公共实例不稳定 | 全部走自托管 Vercel RSSHub |
| 上交 admissions.sjtu.edu.cn 是 SPA | RSSHub 没做 SPA 支持 | 每所大学**预先识别** CMS / SPA / 静态 / API |
| Server 酱免费版限额 5/天 | 推送量 vs 限额 | 39 所合并仍是 1 条/天推送 → 不变 |

---

## 2. 4 阶段流程

```
Phase 1 — 源审计（codex 自动跑，5/12）
Phase 2 — Backup 储备（5/13，仅 P0/P1）
Phase 3 — 实施分波（5/14 ~ 5/24）
Phase 4 — 39 所验收启动（5/25）
```

---

## 3. Phase 1 — 自动化源审计

### 3.1 输出物

`docs/06-源审计报告.md`：39 行 × 10 列表格，作为 Phase 3 实施的唯一输入。

### 3.2 审计脚本规格（给 codex 实现）

新建一次性脚本 `scripts/audit_sources.py`：

**输入**：
- 39 所 985 名单（硬编码或读 `docs/00-方案总览.md` §3.1/3.2 表格）
- RSSHub 自部署实例 URL（`https://rsshub-uni-admission.vercel.app`）
- 招生关键词列表（复用 `classifier.KEYWORDS`，但不含 EXCLUDED_KEYWORDS）

**对每所大学执行**：

```
Step 1: 查 RSSHub namespace 是否存在
  - 已知 28 所有 namespace（见 docs/00-方案总览.md §3.1）
  - 未知 11 所跳过 RSSHub 部分，直接进 Step 4

Step 2: 列出 namespace 下所有路由
  - GET https://api.github.com/repos/DIYgod/RSSHub/contents/lib/routes/<ns>
  - 解析 .ts 文件名得到路由列表
  - 优先级选路：含 zsb/admission/yzb/news 关键词的优先

Step 3: 自动拉取 + 判定覆盖度
  - 对每所最多试 3 条路由（按 Step 2 优先级）
  - GET https://rsshub-uni-admission.vercel.app/<ns>/<route>
  - 解析 RSS 取前 10 条 item title + summary
  - 判定算法：
    - 拒否条件先行：含 EXCLUDED_KEYWORDS（研究生口径）→ 该 item 不计入分子
    - 命中 KEYWORDS（招生口径）→ +1 分子
    - 分母 = 总 item 数
    - 比率 > 50%   → 评级 A（纯招办）
    - 比率 10-50%  → 评级 B（含招生需 LLM）
    - 比率 < 10%   → 评级 C（几乎不含招生）
  - 取最高评级的路由作为代表

Step 4: 找招生官网 URL（仅 P0/P1 需要）
  - 用 DuckDuckGo 或 Bing 搜索 API（如可用）
  - query: "<校名> 本科招生网"
  - 取第一条 .edu.cn 域名结果
  - 不可用则留空，标记为 "manual_needed"

Step 5: 探测招办 CMS 类型（仅 P0/P1 需要）
  - GET 招办首页 HTML（如 Step 4 拿到）
  - 嗅探指纹：
    - 含 "wp_article_list" → webplus
    - 含 "data-n-head-ssr" 或 "_nuxt/" → nuxt-spa
    - 含 "react" + "__NEXT_DATA__" → next-spa
    - 含 <table> 或简单 <ul><li> → static
  - 不明确 → "unknown"

Step 6: 计算优先级
  - rsshub 评级 C → P0（必自写）
  - rsshub 评级 B → P1（自写优先，RSSHub 凑合也行）
  - rsshub 评级 A → P2（暂缓自写）
  - 无 RSSHub namespace + 无 bkzs URL → 标 BLOCKED，人工介入
```

**输出**：
- `docs/06-源审计报告.md`：表格形式
- `docs/06-源审计原始数据.json`：所有抓取的 raw item titles，便于人工抽检

### 3.3 审计脚本质量要求

- 全程**只读**：不写代码、不写 config、不触发推送
- 失败容错：单所失败不影响其他所（参考 fetcher I-1 约定）
- 限速：访问 Vercel RSSHub 间隔 ≥ 2s，避免触发自家限流
- User-Agent 同 `crawler/utils/http.py`：`uni-admission-crawler/0.1 (audit)`
- 耗时预算：39 所 × ~15s/所 ≈ 10 分钟，远低于 NFR-1 180s

---

## 4. Phase 2 — Backup 储备（仅 P0/P1）

### 4.1 范围决策（用户选 B）

| 评级 | 是否做 backup |
|---|---|
| P0（RSSHub C 级，必须自写） | ✅ 必做 |
| P1（RSSHub B 级，自写优先） | ✅ 必做 |
| P2（RSSHub A 级，已够用） | ❌ 暂缓，未来失效再补 |

预估数量：39 所中 P0 ~15-20 所，P1 ~10-15 所，P2 ~5-10 所 → **backup 工作量约 25-35 所**。

### 4.2 每所 P0/P1 建 `docs/sources/<ns>.md`

模板：

```markdown
# 北京大学（pku）

## RSSHub
- namespace: pku
- routes available: news
- 选用: /pku/news
- 实拉 10 条样本（2026-05-12）：
  1. "北大召开党委常委会..." → 非招生
  2. "光华学者讲坛..." → 非招生
  3. "北大新生入学政策..." → 招生
  4-10. ...
- 招生覆盖度: B（30% 含招生）

## 本科招办官网（backup）
- URL: https://admission.pku.edu.cn
- 列表页: /notice/list.htm
- 渲染: static HTML, ul.news-list > li
- 实测 selector 命中: ✅
- 真实抓取样本（2026-05-12，前 5 条）：
  1. "2026 年强基计划报名..." 2026-05-09
  2. "招生宣讲安排..." 2026-05-08
  ...

## 实施决策
- 优先级: P1
- 实施方案: 优先 /pku/news + LLM 过滤；如召回率 < 0.8 切换 admission.pku.edu.cn 自写
- CMS 类型: webplus
- 实施波次: Wave 2（Webplus 子类批次）
```

### 4.3 CMS 分组（用于 Wave 复用）

预期 39 所大致归类（实际以 Phase 1 Step 5 探测为准）：

| CMS | 预期数量 | 复用基类 | 单所工作量 |
|---|---|---|---|
| Webplus | ~15 所 | `WebplusFetcher`（从 FudanAoFetcher 重构） | 10 行子类 |
| Static HTML（自定义）| ~10 所 | `BaseHtmlFetcher`（待抽象） | 30 行 |
| Nuxt/Vue SPA | ~5 所 | `SjtuAdmissionsFetcher` 套路 | 50 行 |
| React/Next SPA | ~3 所 | 同上 | 50 行 |
| 仅 RSSHub | ~6 所 | 无（复用 `RsshubFetcher`） | 0 行 |

---

## 5. Phase 3 — 实施 4 波次

### Wave 1（5/15-5/17）：RSSHub 直接复用（28 所）

- 输入：Phase 1 中评级 A/B 的源
- 工作量：每所只改 `config/config.yaml` 加 1 个 source 块
- 测试：每所加 1 个 `@pytest.mark.live` 测试
- 阻塞性：低（无新代码）

### Wave 2（5/19-5/21）：Webplus CMS 自写（~15 所）

- 抽象 `WebplusFetcher` 基类（重构 FudanAoFetcher）
- 对 Phase 1 探测为 webplus 的每所写子类
- 复用现有 LIST_PATHS / selector 多套兼容机制

### Wave 3（5/22-5/24）：SPA / API 自写（~5-8 所）

- 每所单独调研 XHR API（参考 SJTU 套路）
- 找不到 API 的，2 选 1：
  - 上 Playwright（+200MB 镜像 + 30s 单次运行，仍 < NFR-1 180s）
  - 退回 RSSHub 凑合 + 标记 known_degraded

### Wave 4（5/25）：容量复测 + 39 所验收启动

- Server 酱 desp 长度复测：39 所合并预期日推送 50-150 条 → Markdown 可能超 32KB
  - 应对：按地区分组（在京/在沪/华东/华中/华南/华东北/西部）渲染多个二级标题；如仍超 32KB 触发 split_by_size 自动切分
- Vercel RSSHub 容量复测：28 路由 × 1 次/天 = 28 请求/天，免费层千万级请求绰绰有余
- Gemini Flash 复测：预计 200-300 LLM 调用/天，远低于 1500 RPD
- GitHub Actions 时间复测：fetcher 串行 39 所 × ~5s = ~3 分钟，需改为**并发抓取**（ThreadPoolExecutor）压到 < NFR-1 180s

---

## 6. 关键风险与应对

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| 多所招办用 SPA 但无 XHR API | 中 | 必须上 Playwright | 预算 +200MB 镜像 + 单次运行 +30s |
| 多所招办反爬虫（验证码 / IP 限制）| 中 | 完全无法抓 | 备用 RSSHub 凑合 + 标记降级 |
| 推送量爆涨 → Markdown > 32KB | 高 | Server 酱 desp 截断 | 渲染分组 + split_by_size + 必要时分日推送 |
| 单条消息 39 个 ## 排版混乱 | 高 | 用户体验差 | 渲染模板加地区/类型分组 |
| Gemini Flash 免费层不够 | 低 | 分类降级 | 现实跑出来 < 300 RPD，远低于 1500 |
| 复旦/清华改版同时发生 | 低 | 多源失效 | NFR-6 单源失败隔离 |
| Vercel RSSHub 被招办反爬 | 中 | 多源同时 503 | 加重试 + 监控告警 + 优先 fallback 自写 |
| Phase 1 自动判定误判 A/B/C | 中 | 选错优先级 | 输出 raw data JSON 便于人工抽检；P1 验证前抽样 5 所核对 |

---

## 7. 时间表（按 C 立即启动）

| 日期 | 阶段 | 谁 | 产出 |
|---|---|---|---|
| **5/12（明）** | Phase 1 审计脚本开发 + 跑 | codex | `scripts/audit_sources.py` + `docs/06-源审计报告.md` + raw JSON |
| 5/13 | Phase 1 review + Phase 2 启动 | reviewer + codex | 人工抽检 5 所确认 A/B/C 判定；P0/P1 backup 文件批量生成 |
| 5/14 | Phase 2 完成 | codex | `docs/sources/<ns>.md` × 25-35 所 |
| 5/15-5/17 | POC 验收 D5-D7 + Wave 1 | codex（POC 验收不动）| 28 所 RSSHub 复用接入 |
| 5/18 | POC 验收报告 + Wave 2 启动 | reviewer + codex | DoD 通过；`WebplusFetcher` 基类 |
| 5/19-5/21 | Wave 2 实施 | codex | ~15 所 Webplus 子类 |
| 5/22-5/24 | Wave 3 实施 | codex | ~5-8 所 SPA / API |
| 5/25 | Wave 4 + 39 所验收启动 | codex + 自然 cron | 容量复测；进入 39 所 7 天验收（5/25 ~ 5/31）|
| 6/1 | 39 所验收报告 | reviewer | 项目正式收官 |

**总周期：5/12 启动 → 6/1 完结 = 21 天**

---

## 8. 立即给 codex 的指令（5/12 起动）

> 启动方案 B 升级 Phase 1。需求详见 `docs/07-方案B升级路线.md` §3。
>
> **5/12 任务（限当日完成）**：
>
> 1. 写 `scripts/audit_sources.py`，行为见 §3.2 step 1-6
> 2. 39 所名单从 `docs/00-方案总览.md` 拷贝（§3.1 28 所 + §3.2 11 所）
> 3. 跑一次脚本，输出：
>    - `docs/06-源审计报告.md`：39 行表格
>    - `docs/06-源审计原始数据.json`：raw item titles
> 4. 提交并 push，commit 信息含「Phase 1 audit complete」
> 5. 不影响 POC 当日 09:00 cron 运行（脚本走单独 entry，不入 main 流程）
>
> **不在 5/12 范围**：Phase 2/3/4 等明天审计跑完看到结果再启动。

---

## 9. 7 天 POC 验收保护

升级期不打断 POC 验收：

- POC 现有 5 源（S-01/03/05/06/07）维持运行至 5/17 D+7
- Phase 1/2 只读操作，不改 main 流程
- Wave 1 加入新 source 不删除旧 source，DoD A4「重复推送 = 0」依然成立
- 5/25 升级到 39 所后，再次连续 7 天验收（5/25-5/31）

---

## 10. 收官标准（5/25 起 39 所 DoD）

POC DoD A1-A7 升级为：

- **A1-39** 连续 7 天每日 09:00 ± 5 分钟成功推送
- **A2-39** 人工对比 **39 所**官网招生口公告，漏推率 ≤ 5%（39 所规模无法 0 漏）
- **A3-39** 经分类器后误推 ≤ 10 条/周（条数随源数线性增加）
- **A4-39** 重复推送 = 0 条
- **A5-39** 月度成本 ≤ ¥30（Gemini Flash + Vercel 全免费，仅极端情况付费）
- **A6-39** 任意 5 所同时失败，其他 34 所仍正常推送
- **A7-39** Server 酱微信 7/7 送达；Markdown 分页/截断逻辑无异常
